services:
  rule-api:
    build: .
    container_name: rule-api
    env_file: .env
    environment:
      - PORT=4444
    networks:
      - oas
      - npm-network

networks:
  oas:
    external: true
  npm-network:
    external: true